spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Albert1616/sistema-personal-web-config.git
          search-paths:
            - Gateway
            - Auth
          cloneOnStart: true
server:
  port: 8888
